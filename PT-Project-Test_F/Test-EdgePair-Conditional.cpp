#include <gtest\gtest.h>
#include"..\PT-Project\Statements\Conditional.h"

TEST(ConditionalEdgePairCoverage, updateStatementText) {
	EXPECT_TRUE(true); //edge coverage subsumes edge pair 
}

TEST(ConditionalEdgePairCoverage, setConn) {
	EXPECT_TRUE(true); //edge coverage subsumes edge pair 
}

TEST(ConditionalEdgePairCoverage, save) {
	EXPECT_TRUE(true); //edge coverage subsumes edge pair 
}
TEST(ConditionalEdgePairCoverage, Operator) {
	EXPECT_TRUE(true); //edge coverage subsumes edge pair 
}
TEST(ConditionalEdgePairCoverage, Operator_S) {
	EXPECT_TRUE(true); //edge coverage subsumes edge pair 
}

TEST(ConditionalEdgePairCoverage, Load) {
	EXPECT_TRUE(true); //edge coverage subsumes edge pair 
}



